extends layout

block content
    ul(id="messages")
        form(action="")
            input(id="m" autocomplete="off")
            button="Send"
            |
    script(src="https://code.jquery.com/jquery-1.11.1.js")
    script(src="js/bundle.js")